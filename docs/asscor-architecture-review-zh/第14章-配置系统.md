# 第 14 章 — 配置系统

配置定义运行时的行为。在 Asscor 中，配置被视为受管理的运行时资源而非静态文件。

配置存在于多个层次：全局运行时 → 内核 → 基础设施服务 → 插件 → 评估引擎。每层仅拥有自己的配置。配置遵循受管理的生命周期：加载 → 解析 → 验证 → 归一化 → 发布 → 使用。

无效配置永远不应进入运行时。配置变更不一定要求重启运行时。每个子系统应仅访问与其职责相关的配置。配置变更自然与事件总线集成。

配置常包含敏感的运营信息——应最终支持加密配置、密钥分离和权限控制。应避免的风险：配置漂移、隐藏默认值、跨层依赖、运行时变异。
